interface:
  display_name: "Hvgapp"
  short_description: "Repo-specific patterns and workflows for hvgapp"
  default_prompt: "Use the hvgapp repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true